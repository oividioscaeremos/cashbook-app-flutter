import 'package:flutter/material.dart';

const zeroAllPadding = EdgeInsets.all(0.0);

const tenAllPadding = EdgeInsets.all(10.0);

const tenRightPadding = EdgeInsets.only(right: 10.0);

const tenLeftPadding = EdgeInsets.only(left: 10.0);

const tenWidthSizedBox = SizedBox(width: 10.0);

const tenSymmetricPadding = EdgeInsets.symmetric(vertical: 10.0);

const EdgeInsets kTransactionPadding = EdgeInsets.all(15.0);

const EdgeInsets kHorizontalTwenty = EdgeInsets.symmetric(horizontal: 20.0);

const TextStyle kUpcomingRevTextStyle = TextStyle(
  fontSize: 20.0,
  fontWeight: FontWeight.bold,
);

const double kAppBarHeight = 60.0;

const TextStyle kAlertButtonTextStyle =
    TextStyle(color: Colors.white, fontSize: 20);

const Color kAlertColor = Color.fromRGBO(0, 179, 134, 1.0);

const LinearGradient kAlertGradient = LinearGradient(colors: [
  Color.fromRGBO(116, 116, 191, 1.0),
  Color.fromRGBO(52, 138, 199, 1.0)
]);

const TextStyle kCompanyNameTextStyle = TextStyle(
  fontSize: 18.0,
  fontWeight: FontWeight.bold,
);

const TextStyle kAddPartnerPersonNameTextStyle =
    TextStyle(fontSize: 16.0, fontWeight: FontWeight.bold);

const TextStyle kProfileNameAndSurnameTextStyle = TextStyle(
  fontWeight: FontWeight.bold,
);

const LinearGradient kGetReportGradient = LinearGradient(
  colors: [
    Color(0xffffac84),
    Color(0xffff795a),
  ],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const BoxConstraints kBoxConstraints = BoxConstraints(
  minWidth: 88.0,
  minHeight: 46.0,
);

const BoxConstraints kBoxConstraintsv2 = BoxConstraints(
  minWidth: 88.0,
  minHeight: 36.0,
);

const TextStyle kOnlyBoldTextStyle = TextStyle(
  fontWeight: FontWeight.bold,
);

const List<Color> kColorArrayRev = [
  Color(0xff08bcff),
  Color(0xff08bcdd),
];

const List<Color> kColorArrayPay = [
  Color(0xffffac84),
  Color(0xffff795a),
];

const SizedBox kTwentyHeightSizedBox = SizedBox(
  height: 20.0,
);

const LinearGradient kLinearGradientForOKBox = LinearGradient(colors: [
  Color.fromRGBO(116, 116, 191, 1.0),
  Color.fromRGBO(52, 138, 199, 1.0)
]);

const TextStyle kRegistrationStyle =
    TextStyle(color: Colors.white, fontSize: 20);
