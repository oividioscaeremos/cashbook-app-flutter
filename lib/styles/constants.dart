import 'package:flutter/material.dart';

const tenAllPadding = EdgeInsets.all(10.0);

const tenRightPadding = EdgeInsets.only(right: 10.0);

const tenLeftPadding = EdgeInsets.only(left: 10.0);

const tenWidthSizedBox = SizedBox(width: 10.0);

const tenSymmetricPadding = EdgeInsets.symmetric(vertical: 10.0);
